# Display parameter information in a human-readable format.

Display parameter information in a human-readable format.

## Usage

``` r
display_param_name(group, name, index)
```

## Arguments

- group:

  The group name (can be NA).

- name:

  The parameter name.

- index:

  The index of the parameter (can be NA).
