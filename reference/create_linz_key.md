# Create LINZ API key

This function will open a browser window to create a LINZ account and
obtain an API key. The API key will be copied to the clipboard and can
be added to the environment variables using the
[`add_linz_key()`](add_linz_key.md) function.

## Usage

``` r
create_linz_key()
```
