% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pest_files.R
\name{write_pest_tpl}
\alias{write_pest_tpl}
\title{Write the PEST template (\code{.tpl}) file}
\usage{
write_pest_tpl(par_tbl, ctrl, width = 23L)
}
\arguments{
\item{par_tbl}{dataframe; from \code{\link{pest_param_table}}.}

\item{ctrl}{list; from \code{\link{create_pest_control}}.}

\item{width}{Integer. Total width of each template field, including the
two \code{~} delimiters. PEST writes the parameter value right-justified into
this field, so the width caps the precision PEST can express: 10 is the
documented minimum and 23 (the default here) is wide enough for a
full-precision double, which matters for parameters whose plausible
range spans orders of magnitude.}
}
\value{
Invisibly, a length-one named character vector
\code{c("<template file>" = "<model input file>")}, ready to pass to
\code{\link{write_pst}} as \code{tpl_files}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Writes a PEST template file for a single side-car parameter CSV, rather
than templating the model configuration files (\code{glm4.nml}, \code{gotm.yaml},
\code{simstrat.par}, ...) directly.

This is a deliberate choice. \code{AEME::input_model_parameters()} does not
simply substitute numbers into config files:
\itemize{
\item parameters whose \code{file} is \code{"met"}, \code{"inf"} or \code{"wdr"} are \strong{multipliers
applied to entire driver time series}, and rewrite the meteorology,
inflow and outflow data files row by row. There is no single field in a
text file for PEST to write into.
\item config parameters are written via \code{read_nml()}/\code{set_nml()}/\code{write_nml()},
\code{yaml::write_yaml()} and \code{write_aed_param_csv()}, so the on-disk number
formatting (precision, scientific notation, field width) is decided by
those writers and could change between AEME versions - silently breaking
any template built by matching on the previous formatting.
\item \code{aed2_phyto_pars.nml} / \code{aed2_zoop_pars.nml} parameters are located by
index into a group list read from \code{aed2.nml}, not by name.
}

Templating one CSV that the forward run reads sidesteps all of this: PEST
owns a file whose format we control completely, and
\code{\link{pest_forward_run}} hands the values to the same
\code{input_model_parameters()} path that \code{\link{run_aeme_param}} already
uses, so there is no duplicated file-writing logic to drift.
}
\seealso{
\code{\link[=write_pest_ins]{write_pest_ins()}}, \code{\link[=write_pest_forward_run]{write_pest_forward_run()}}
}
