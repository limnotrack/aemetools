% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_sen_control.R
\name{create_sen_control}
\alias{create_sen_control}
\title{Create control list for a PEST-driven sensitivity analysis}
\usage{
create_sen_control(
  file_type = "db",
  file_name = NULL,
  file_dir = "calib_sa",
  na_value = 999,
  parallel = TRUE,
  ncore = parallel::detectCores() - 1,
  timeout = Inf,
  sen_method = "morris",
  morris_r = 4,
  morris_p = 4,
  morris_delta = NULL,
  pest_dir = "pest",
  case = "aeme",
  port = 4004,
  solver_timeout = 24 * 3600,
  stall_minutes = 10,
  derinc = 0.01,
  derinc_lb = 1e-06,
  pestpp_options = list(),
  overwrite = TRUE,
  keep_files = TRUE,
  trim_output = TRUE,
  vars_sim,
  ...
)
}
\arguments{
\item{file_type}{Character. Output type: \code{"csv"} or \code{"db"}.
Default \code{"db"}.}

\item{file_name}{Character. Output file name. Defaults to
\code{"results.db"} (db) or \code{"simulation_metadata.csv"} (csv).}

\item{file_dir}{Character. Output directory. Default \code{"calib_sa"}.}

\item{na_value}{Numeric. Penalty value substituted for \code{NA} fit values
during optimisation to discourage parameter sets that produce invalid
model output. Default \code{999}.}

\item{parallel}{Logical. Run the model evaluations in parallel? Default
\code{TRUE}. PEST++ parallelises only through a run manager, so this selects
the PANTHER manager (a master plus \code{ncore} agents) rather than a serial
run in which the master evaluates the model itself. aemetools does
\strong{not} create its own \code{parallel::makeCluster()} for a PEST++ run;
\code{ncore} is the number of PANTHER agents.}

\item{ncore}{Integer. Number of cores if \code{parallel = TRUE}.
Default \code{parallel::detectCores() - 1}.}

\item{timeout}{Numeric. Max runtime in seconds. Default \code{Inf}.}

\item{sen_method}{Character. Currently only \code{"morris"}.}

\item{morris_r}{Integer. Number of Morris trajectories (repetitions).
Default \code{4}. Model-run cost is \code{(n_par + 1) * morris_r}.}

\item{morris_p}{Integer. Number of levels in the Morris sampling grid.
Default \code{4}.}

\item{morris_delta}{Numeric. Morris step, as a fraction of the parameter
range. \code{NULL} (the default) lets \code{pestpp-sen} pick \code{p / (2 * (p - 1))}.}

\item{pest_dir}{Character. Working directory for the generated \code{.pst},
\code{.tpl}, \code{.ins} and forward-run files. Defaults to \code{"pest"} under the
lake directory. This directory is created and, if \code{overwrite = TRUE},
cleared on each run.}

\item{case}{Character. Basename for the generated files, so the control
file is \verb{<case>.pst}. Default \code{"aeme"}.}

\item{port}{Integer. TCP port for the PANTHER master. Default \code{4004}.}

\item{solver_timeout}{Numeric. Maximum seconds to wait for the solver to
finish before aborting. Default \code{86400} (24 hours). This is a backstop
against a detached PANTHER master that neither completes nor reports an
error; it is not a per-model-run limit, which is \code{timeout}.}

\item{stall_minutes}{Numeric. Abort if the solver goes this many minutes
without completing a model run or writing to its record file. Default
\code{10}. This is the practical guard: PEST++ can finish every run,
terminate its agents and then fail to exit, spinning at 100\% CPU with
its output unflushed, which \code{solver_timeout} alone would not catch for
a further day. If every expected run is already logged the results are
kept and the run continues; otherwise it aborts. Raise this if a single
model evaluation legitimately takes longer than 10 minutes.}

\item{derinc, derinc_lb}{Numeric. Finite-difference increment (as a
fraction of the parameter value) and its absolute lower bound, used by
\code{"pestpp-glm"} and \code{"pestpp-sen"}. Defaults \code{0.01} and \code{1e-6}.}

\item{pestpp_options}{Named list. Additional \verb{++} options written verbatim
to the control file, e.g.
\code{list(ies_bad_phi_sigma = 2.0, ies_autoadaloc = TRUE)}. Names are
emitted as \code{++name(value)}, and override the defaults aemetools sets.

\code{ies_drop_conflicts} is one worth knowing about. It is \strong{off} by
default: \code{pestpp-ies} runs with no observation noise unless an
obs-noise-specific option is given, and against noiseless observations
the prior-data-conflict test flags nearly everything - dropping which
can leave nothing to fit, aborting the run. Conflicts are still
detected and readable with \code{\link{pest_prior_data_conflict}}. Turn
it on once \code{noise_sd} makes the test meaningful.}

\item{overwrite}{Logical. Clear \code{pest_dir} before writing? Default \code{TRUE}.}

\item{keep_files}{Logical. Retain the PEST working directory after the run
completes, for inspection or restart. Default \code{TRUE}.}

\item{trim_output}{Logical. Before staging the model for PEST++, restrict
each model's written output to \code{vars_sim} via
\code{\link[AEME]{set_output_vars}} (and switch off GLM's
\code{mass_balance.csv}). Default \code{TRUE}. Rewrites the model configuration in
the lake directory; the trimmed config is what gets copied to the PANTHER
agent directories.}

\item{vars_sim}{Named list describing the output variables, exactly as for
\code{\link[=create_sa_control]{create_sa_control()}}. Each element is
\verb{list(var = <AEME var>, month = <ints>, depth_range = <c(lo, hi)>)}; one
Morris response is produced per named element.}

\item{...}{Must be empty. Additional arguments are not allowed.}
}
\value{
A \code{calib_sa_control} list with \code{method = "sa"}, \code{engine = "pest"}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Builds the control object for running a global sensitivity analysis with
the external \code{pestpp-sen} engine instead of the built-in Sobol' sampler
used by \code{\link[=sa_aeme]{sa_aeme()}} / \code{\link[=create_sa_control]{create_sa_control()}}. The returned object is a
\code{calib_sa_control} with \code{method = "sa"} and \code{engine = "pest"}, so the
existing sensitivity read/plot pipeline still recognises the run while
\code{\link[=sa_aeme]{sa_aeme()}} hands the evaluation loop to PEST++.

The only method currently supported is the \strong{Method of Morris}
(elementary effects): global screening at \code{(n_par + 1) * morris_r} model
runs, tolerant of a noisy or nonlinear model response. It returns, per
parameter per variable, \code{mu_star} (overall influence) and \code{sigma}
(nonlinearity / interaction).

Unlike the calibration path, the sensitivity path always samples the
parameters \strong{linearly} on \verb{[min, max]} (\code{partrans = "none"}), so the
Morris indices are directly comparable with a built-in \code{\link[=sa_aeme]{sa_aeme()}} run on
the same \code{param}.
}
\examples{
ctrl <- create_sen_control(
  morris_r = 8, ncore = 4,
  vars_sim = list(surf_temp = list(var = "HYD_temp", month = c(12, 1, 2),
                                   depth_range = c(0, 2)))
)
ctrl
}
\seealso{
\code{\link[=sa_aeme]{sa_aeme()}}, \code{\link[=create_sa_control]{create_sa_control()}}, \code{\link[=read_sen]{read_sen()}}, \code{\link[=plot_sen]{plot_sen()}},
\code{\link[=install_pest]{install_pest()}}
}
