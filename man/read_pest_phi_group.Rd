% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pest_diagnostics.R
\name{read_pest_phi_group}
\alias{read_pest_phi_group}
\title{Read the \code{pestpp-ies} per-group objective-function trajectory}
\usage{
read_pest_phi_group(ctrl)
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}
}
\value{
A long dataframe: \code{iteration}, \code{total_runs}, \code{obgnme}, \code{var_aeme}
and \code{phi} (the group's mean contribution across the ensemble at that
iteration).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{\link{read_pest_phi}} gives the total phi per iteration;
\verb{<case>.phi.group.csv} breaks that down by observation group. Since
\code{\link{pest_obs_table}} puts one group per \code{vars_sim} variable, this
shows whether - say - temperature is still improving while dissolved
oxygen has stalled, which the aggregate hides.
}
\seealso{
\code{\link[=read_pest_phi]{read_pest_phi()}}, \code{\link[=plot_pest_phi_group]{plot_pest_phi_group()}}
}
