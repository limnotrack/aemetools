% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_pest_control.R
\name{pest_expected_runs}
\alias{pest_expected_runs}
\title{Expected number of forward model runs for a PEST++ control}
\usage{
pest_expected_runs(ctrl, n_par = NULL)
}
\arguments{
\item{ctrl}{list; from \code{\link{create_pest_control}} or
\code{\link{create_sen_control}}.}

\item{n_par}{Integer. Number of adjustable parameters. Needed for
\code{"pestpp-glm"} and \code{"pestpp-sen"}.}
}
\value{
Numeric; expected model runs, or \code{NA} when it cannot be
determined from the control alone.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

How many times the model will actually be run, so the cost of a
configuration is visible before it is started. Unlike the built-in
methods - where \code{itermax} is a budget you set and \code{ngen} follows from it
\itemize{
\item a PEST++ run's cost falls out of the solver settings, so this is
computed rather than chosen.
}

For \code{"pestpp-ies"} the figure is exact, and follows the accounting in
\code{EnsembleMethod::solve()}:
\itemize{
\item the prior ensemble costs \code{ies_num_reals} runs. \code{ies_include_base}
does not add to that - \code{add_bases()} drops the last realisation before
appending the \code{base} one - so the ensemble is always \code{ies_num_reals}.
\item each iteration then tests every lambda/scale-factor combination on a
\emph{subset} of realisations, costing
\verb{n_lambda x n_scale x subset_size} runs, and applies the winning
combination to the rest, costing \code{ies_num_reals - subset_size}.
}

A negative \code{ies_subset_size} (the default, \code{-10}) is a percentage of the
ensemble, floored at 4 realisations. That floor is why very small
ensembles are inefficient: at \code{ies_num_reals = 6} the subset is 4 of 6,
so lambda testing costs more runs than there are realisations.

For \code{"pestpp-glm"} the cost is driven by the finite-difference Jacobian,
\code{n_par + 1} runs per iteration, and \code{n_par} is only known once the
parameters are, so \code{n_par} must be supplied. The result is a lower
bound: each iteration may add up to \code{NUMLAM} further upgrade runs.

For \code{"pestpp-sen"} with the Method of Morris the figure is exact once
\code{n_par} is known: \code{(n_par + 1) * gsa_morris_r} trajectories. \code{"pestpp-swp"}
sweeps a user-supplied table the control knows nothing about, so returns
\code{NA}.
}
\examples{
# Defaults: 50 prior runs, then 6 iterations of (1 x 3 x 5) + (50 - 5).
pest_expected_runs(create_pest_control(ncore = 1))
}
