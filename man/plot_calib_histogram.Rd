% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_calib_histogram.R
\name{plot_calib_histogram}
\alias{plot_calib_histogram}
\title{Plot calibration histogram plot}
\usage{
plot_calib_histogram(
  calib,
  fit_col = "fit",
  nrow = 2,
  base_size = 8,
  plot_data = NULL
)
}
\arguments{
\item{calib}{A list with the calibration results loaded using
\code{\link{read_calib}}.}

\item{fit_col}{character; name of column containing fit values. Default is
\code{"fit"}.}

\item{nrow}{integer; number of rows in plot}

\item{base_size}{numeric; base size for theme}

\item{plot_data}{list; optional, pre-computed via the internal
\code{prepare_calib_plot_data()} helper. When supplied, \code{calib}/\code{fit_col} are
not re-queried. Used by \code{\link[=plot_calib]{plot_calib()}} so the underlying data is only
prepared once for all three plot types; leave as \code{NULL} (the default)
when calling this function directly.}
}
\value{
patchwork object with one histogram plot per simulation ID.
}
\description{
Plots a histogram of every simulated parameter value, filled by
generation, with the best value per parameter marked.
}
\examples{
\dontrun{
calib <- read_calib(sim_id = sim_id, ctrl = ctrl)
plot_calib_histogram(calib = calib)
}
}
