% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_pest_output.R
\name{read_pest_phi}
\alias{read_pest_phi}
\title{Read the PEST++ objective-function trajectory}
\usage{
read_pest_phi(ctrl, type = "actual")
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}

\item{type}{Character. Which phi to read. \code{"actual"} (default) is
measured against the observed values; \code{"meas"} is against the noise
realisations each ensemble member was assigned; \code{"composite"} adds the
regularisation term when \code{ies_reg_factor} is non-zero.}
}
\value{
A dataframe with \code{iteration}, \code{total_runs}, \code{mean},
\code{standard_deviation}, \code{min} and \code{max}, plus one column per
realisation.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reads \verb{<case>.phi.actual.csv}, which \code{L2PhiHandler::write()} appends one
row to per iteration. This is how a run's convergence is judged: phi is
the weighted sum of squared residuals, so a falling \code{min}/\code{mean} means
the ensemble is fitting the observations better, and a flattening
trajectory means further iterations are not buying anything.
}
\seealso{
\code{\link[=plot_pest_phi]{plot_pest_phi()}}, \code{\link[=read_pest_ensemble]{read_pest_ensemble()}}
}
