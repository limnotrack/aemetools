% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pest_prior.R
\name{pest_prior_ensemble}
\alias{pest_prior_ensemble}
\title{Draw a prior parameter ensemble for \code{pestpp-ies}}
\usage{
pest_prior_ensemble(
  param,
  n = 100,
  dist = "uniform",
  cov = NULL,
  sigma_range = 4,
  seed = NULL,
  include_base = TRUE,
  real_names = NULL,
  file = NULL
)
}
\arguments{
\item{param}{dataframe; as passed to \code{\link{calib_aeme}} - see
\code{\link{pest_param_table}} for the required columns.}

\item{n}{Integer. Ensemble size (number of realisations). Default \code{100}.}

\item{dist}{Character. \code{"uniform"} (independent uniforms on the bounds),
\code{"normal"} (Gaussian about \code{value}), or \code{"triangular"} (mode at
\code{value}, limits at the bounds). Log parameters are drawn in log10 space
and back-transformed.}

\item{cov}{Optional. A prior covariance matrix from
\code{\link{pest_prior_cov}} (or a path to one). Only used when
\code{dist = "normal"}: its diagonal sets the per-parameter spread, and a
non-diagonal matrix draws correlated realisations via
\code{\link[MASS]{mvrnorm}}.}

\item{sigma_range}{Numeric. Number of standard deviations spanned by the
\verb{[min, max]} range. Default \code{4}, the \code{pyemu} default (so \code{sd} is a
quarter of the range, or of the log10 range for a log parameter).}

\item{seed}{Integer or \code{NULL}. Seed for the draw, for reproducibility.}

\item{include_base}{Logical. Replace the last realisation with the initial
parameter values and name it \code{base}, mirroring \code{pestpp-ies}'
\code{ies_include_base}. Keeps the ensemble size at \code{n}. Default \code{TRUE}.}

\item{real_names}{Character or \code{NULL}. Realisation names. Defaults to
\verb{real_0 ... real_\{n-1\}} (with the last renamed \code{base} when
\code{include_base}). Supply this to align a parameter and an observation
ensemble, which \code{pestpp-ies} matches by name.}

\item{file}{Character or \code{NULL}. Path to write the ensemble CSV to, in the
\verb{real_name, <par>, <par>, ...} layout \code{pestpp-ies} expects.}
}
\value{
Invisibly, a dataframe with a \code{real_name} column and one column
per \code{parnme}, values in native (untransformed) units.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Generates the initial (prior) parameter ensemble in R instead of letting
\code{pestpp-ies} draw it internally from independent uniforms on the bounds.
This buys three things the internal draw cannot: a fixed \code{seed} for
reproducibility, a non-uniform prior (Gaussian about the initial values,
or triangular), and an optional prior covariance so parameters can be
drawn correlated.

Pass the written file to \code{pestpp-ies} as \code{++ies_parameter_ensemble(...)}.
\code{\link{calib_aeme}} does this automatically when
\code{\link{create_pest_control}} is given a \code{seed}, a \code{prior_cov}, or a
non-uniform \code{prior_dist}.
}
\seealso{
\code{\link[=pest_obs_ensemble]{pest_obs_ensemble()}}, \code{\link[=pest_prior_cov]{pest_prior_cov()}}, \code{\link[=create_pest_control]{create_pest_control()}}
}
