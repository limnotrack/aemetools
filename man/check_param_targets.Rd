% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_param_targets.R
\name{check_param_targets}
\alias{check_param_targets}
\title{Check that calibration parameters exist in the built model}
\usage{
check_param_targets(param, aeme, model = NULL, error = FALSE)
}
\arguments{
\item{param}{dataframe; as passed to \code{\link{calib_aeme}}, requiring
\code{name} and, for the message, \code{file}.}

\item{aeme}{A built \code{aeme} object.}

\item{model}{Character or \code{NULL}. Which configured model to check
against. \code{NULL} (default) takes the single \code{param$model}, or the first
model \code{AEME::configuration(aeme)} carries.}

\item{error}{Logical. Abort when any parameter is unmatched, instead of
warning and returning them. Default \code{FALSE}.}
}
\value{
Invisibly, the subset of \code{param} rows whose \code{name} matched no key
in the built model (a zero-row dataframe when everything resolved).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Every row of a \code{param} dataframe names a field in one of the model's
configuration files. \code{\link{calib_aeme}} and \code{\link{sa_aeme}}
write those fields through \code{AEME::input_model_parameters()}, which matches
on name: a name the built model does not carry is either a hard error
inside a forward run (or a PEST worker) or a silent no-op. Neither is an
obvious way to find a typo, or a module that was never switched on.

\code{check_param_targets()} compares each parameter name against the keys
present in \code{AEME::configuration(aeme)[[model]]} - every namelist / YAML
field, plus the row keys of any table-valued block such as the AED
phytoplankton and zooplankton parameter tables - and returns the rows
that match nothing. Run it after \code{AEME::build_aeme()} and before the
first \code{calib_aeme()} / \code{sa_aeme()} call.

The comparison is by bare name (any \code{block/} or \code{phyto/} qualifier is
stripped first), so it catches typos and inactive modules but not a
parameter placed in the wrong namelist block.
}
\examples{
\dontrun{
aeme <- AEME::build_aeme(path = path, aeme = aeme, model = "glm_aed",
                         model_controls = mc, use_bgc = TRUE)
check_param_targets(param, aeme, error = TRUE)
}
}
\seealso{
\code{\link[=validate_aeme]{validate_aeme()}}, \code{\link[=calib_aeme]{calib_aeme()}}, \code{\link[=freeze_param]{freeze_param()}}
}
