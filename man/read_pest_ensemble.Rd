% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_pest_output.R
\name{read_pest_ensemble}
\alias{read_pest_ensemble}
\title{Read a PEST++ parameter or observation ensemble}
\usage{
read_pest_ensemble(ctrl, iteration = NULL, type = "par")
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}

\item{iteration}{Integer. Which iteration to read. \code{NULL} (the default)
reads the last one written, i.e. the posterior.}

\item{type}{Character. \code{"par"} for the parameter ensemble, \code{"obs"} for
the simulated observation ensemble.}
}
\value{
A long dataframe with an \code{is_base} flag marking the \code{base}
realisation (the initial parameter values, carried through the run by
\code{ies_include_base}). For \code{"par"}: \code{realisation}, \code{iteration}, \code{is_base},
\code{name_full}, \code{value}. For \code{"obs"}: \code{realisation}, \code{iteration},
\code{is_base}, \code{obsnme}, \code{var_aeme}, \code{Date}, \code{depth}, \code{model}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reads one of the ensembles the solver writes per iteration, translating
the synthetic PEST names back to aemetools identities: parameters become
\code{name_full} (\code{group/name[index]}), observations become their
\code{var_aeme}, \code{Date} and \code{depth}.

The \strong{final} parameter ensemble is the posterior - the product of an
iterative ensemble smoother run, and what you push back through the
model to get predictive uncertainty. Iteration \code{0} is the prior, so
reading both gives the prior-to-posterior comparison that shows which
parameters the observations actually informed.
}
\seealso{
\code{\link[=pest_param_summary]{pest_param_summary()}}, \code{\link[=pest_residuals]{pest_residuals()}}
}
