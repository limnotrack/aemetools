% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pest_prior.R
\name{pest_obs_ensemble}
\alias{pest_obs_ensemble}
\title{Draw an observation (measurement-noise) ensemble for \code{pestpp-ies}}
\usage{
pest_obs_ensemble(
  obs_tbl,
  n = 100,
  noise_sd = NULL,
  seed = NULL,
  include_base = TRUE,
  real_names = NULL,
  file = NULL
)
}
\arguments{
\item{obs_tbl}{dataframe; from \code{\link{pest_obs_table}}, carrying its
\code{map} attribute.}

\item{n}{Integer. Ensemble size. Must match the parameter ensemble.
Default \code{100}.}

\item{noise_sd}{Named numeric vector, keyed by \code{var_aeme}, giving the
measurement standard deviation for each variable in data units (e.g.
\code{c(HYD_temp = 0.5)}). When \code{NULL}, the standard deviation is taken as
\code{1/weight} per observation and a warning is issued.}

\item{seed}{Integer or \code{NULL}. Seed for the draw.}

\item{include_base}{Logical. Replace the last realisation with the
noise-free observed values and name it \code{base}. Default \code{TRUE}.}

\item{real_names}{Character or \code{NULL}. Realisation names; must equal the
parameter ensemble's. Defaults as in \code{\link{pest_prior_ensemble}}.}

\item{file}{Character or \code{NULL}. Path to write the ensemble CSV to.}
}
\value{
Invisibly, a dataframe with a \code{real_name} column and one column
per \code{obsnme}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Builds the observation ensemble \code{pestpp-ies} uses to represent
measurement noise, so that the noise is set from a physical measurement
error rather than inferred from the PEST weights - which, after
\code{\link{pest_obs_table}}'s \code{"balanced"} weighting, no longer carry a
meaningful \code{1/sigma} interpretation.

Pass the written file as \code{++ies_observation_ensemble(...)}.
\code{\link{calib_aeme}} does this automatically when
\code{\link{create_pest_control}} is given \code{noise_sd}.
}
\seealso{
\code{\link[=pest_prior_ensemble]{pest_prior_ensemble()}}, \code{\link[=create_pest_control]{create_pest_control()}}
}
