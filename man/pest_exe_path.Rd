% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_pest.R
\name{pest_exe_path}
\alias{pest_exe_path}
\title{Locate a PEST++ executable}
\usage{
pest_exe_path(exe = "pestpp-ies", dir = pest_install_dir(), error = TRUE)
}
\arguments{
\item{exe}{Character. Executable name, e.g. \code{"pestpp-ies"}.}

\item{dir}{Character. Installation directory to search. Defaults to
\code{\link[=pest_install_dir]{pest_install_dir()}}.}

\item{error}{Logical. Abort with an install hint if not found?
Default \code{TRUE}.}
}
\value{
Character path to the executable, or \code{NA_character_} when
\code{error = FALSE} and it cannot be found.
}
\description{
Looks first in the aemetools installation directory (see
\code{\link[=install_pest]{install_pest()}}), then falls back to the system \code{PATH} so that a
site-wide or conda-installed PEST++ is picked up automatically.
}
