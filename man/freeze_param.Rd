% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freeze_param.R
\name{freeze_param}
\alias{freeze_param}
\title{Freeze calibration parameters at their current value}
\usage{
freeze_param(param, names = NULL)
}
\arguments{
\item{param}{dataframe; as passed to \code{\link{calib_aeme}}, requiring
\code{value}, and \code{name} when \code{names} is used.}

\item{names}{Character or \code{NULL}. Which \code{param$name} entries to freeze.
\code{NULL} (default) freezes every row. A name not present in \code{param} is an
error rather than a silent no-op.}
}
\value{
\code{param}, with \verb{min == max == value} for the selected rows.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Sets \code{min} and \code{max} equal to \code{value} for the selected parameters (every
row when \code{names} is \code{NULL}). A frozen parameter is still passed to
\code{\link{calib_aeme}} and \code{\link{sa_aeme}}:
\itemize{
\item the built-in engines write it to the model configuration and drop it
from the search;
\item the PEST engines keep it in the control file as \code{partrans = "fixed"},
so it stays visible in the parameter map, the \code{pestpp-ies} ensembles,
\code{pest_param_summary()} and the sensitivity output.
}

This is the mechanism for carrying an earlier stage of a staged
calibration forward as a constant - see \code{\link{carry_param}} for the
common case of freezing the winners of a finished run.
}
\examples{
param <- data.frame(
  model = "glm_aed", file = "glm3.nml", group = "light",
  name = c("Kw", "ce"), index = NA_real_,
  value = c(0.5, 0.0013), min = c(0.1, 5e-4), max = c(1.5, 5e-3)
)
freeze_param(param, names = "Kw")
}
\seealso{
\code{\link[=carry_param]{carry_param()}}, \code{\link[=pest_param_table]{pest_param_table()}}, \code{\link[=set_param_log]{set_param_log()}}
}
