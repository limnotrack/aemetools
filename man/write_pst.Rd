% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pst.R
\name{write_pst}
\alias{write_pst}
\title{Write a PEST control (\code{.pst}) file}
\usage{
write_pst(
  par_tbl,
  obs_tbl,
  ctrl,
  tpl_files,
  ins_files,
  model_command,
  file = NULL,
  prior_info = character()
)
}
\arguments{
\item{par_tbl}{dataframe; from \code{\link{pest_param_table}}.}

\item{obs_tbl}{dataframe; from \code{\link{pest_obs_table}}.}

\item{ctrl}{list; from \code{\link{create_pest_control}}.}

\item{tpl_files}{Named character vector; template files as
\code{c("<template file>" = "<model input file>")}.}

\item{ins_files}{Named character vector; instruction files as
\code{c("<instruction file>" = "<model output file>")}.}

\item{model_command}{Character. The command PEST++ runs for a single
forward model evaluation, e.g.
\code{"Rscript forward_run.R"}.}

\item{file}{Character. Path of the \code{.pst} file to write. Defaults to
\verb{<ctrl$pest_dir>/<ctrl$case>.pst}.}

\item{prior_info}{Character vector. Optional prior-information equations
written verbatim to the \verb{* prior information} section (Tikhonov
regularisation).}
}
\value{
Invisibly, the path of the file written.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Writes a PEST/PEST++ control file from the parameter and observation
tables built by \code{\link{pest_param_table}} and
\code{\link{pest_obs_table}}. Section ordering and the fixed line
structure of the \verb{* control data} block follow the PEST control-file
specification, which PEST++ reads unchanged.
}
\seealso{
\code{\link[=create_pest_control]{create_pest_control()}}, \code{\link[=install_pest]{install_pest()}}
}
