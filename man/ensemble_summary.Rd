% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble_summary.R
\name{ensemble_summary}
\alias{ensemble_summary}
\alias{as.data.frame.aeme_ensemble_summary}
\title{Summarise an AEME ensemble across members}
\usage{
ensemble_summary(
  aeme,
  model,
  vars_sim = "HYD_temp",
  depths = NULL,
  probs = c(0.025, 0.25, 0.5, 0.75, 0.975),
  remove_spin_up = TRUE,
  add_obs = TRUE,
  depth_ref = c("surface", "bottom"),
  obs_tol = 0.5,
  keep_members = TRUE
)

\method{as.data.frame}{aeme_ensemble_summary}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{aeme}{an \code{aeme} object with ensemble output, or an
\code{\link[=ensemble_summary]{ensemble_summary()}}.}

\item{model}{character; model(s) to plot. Optional when \code{aeme} is an
\code{\link[=ensemble_summary]{ensemble_summary()}}.}

\item{vars_sim}{character; one or more AEME variables to summarise.}

\item{depths}{numeric; depths (m, referenced to \code{depth_ref}) at which to
summarise depth-resolved variables. \code{NULL} (default) pools all layers for
a depth-resolved variable - matching the historical \code{\link[=plot_ensemble]{plot_ensemble()}}
behaviour - and is the only sensible value for a 1-D variable such as
\code{LKE_lvlwtr} or \code{HYD_thmcln}.}

\item{probs}{numeric; quantile probabilities for the summary bands. \code{0.5}
is always added so a median is available.}

\item{remove_spin_up}{logical; remove spin-up period from plot. Default is
TRUE.}

\item{add_obs}{logical; add observations to plot}

\item{depth_ref}{character; \code{"surface"} (default) or \code{"bottom"}, passed to
\code{\link[AEME:get_var]{AEME::get_var()}} and used to align observations.}

\item{obs_tol}{numeric; half-width (m) of the window used to attach
observations to each requested depth. Default 0.5.}

\item{keep_members}{logical; keep the extracted per-member frame on the
result. Default \code{TRUE}. \code{FALSE} drops it - smaller object, but
\code{plot_ensemble(type = "line")} and future member-level scores then can't
be computed from it.}

\item{x}{an \code{aeme_ensemble_summary}.}

\item{row.names, optional}{passed to the data.frame method; ignored.}

\item{...}{unused.}
}
\value{
An \code{aeme_ensemble_summary} object: a list of \code{stats}, \code{members}
(or \code{NULL}) and \code{obs} (or \code{NULL}), with \code{probs}, \code{depths}, \code{depth_ref},
\code{vars_sim}, \code{vars_2d}, \code{n_members} and \code{period} attributes.
}
\description{
\code{\link[=run_aeme_ensemble]{run_aeme_ensemble()}} stores every member's full model output on the
\code{aeme} object. Turning that into a plot or a skill score means, each time,
looping over the members and pulling the variable out with
\code{\link[AEME:get_var]{AEME::get_var()}} - the expensive step. \code{ensemble_summary()} does that
extraction \strong{once} and returns a tidy object holding
\itemize{
\item \code{stats} - one row per date (and depth, and model, and variable) with the
ensemble \code{mean}, \code{sd}, \code{n} and the quantiles named in \code{probs};
\item \code{members} - the extracted long frame the statistics were built from
(\code{Date}, \code{depth}, \code{value}, \code{Model}, \code{var_sim}, \code{ens}), kept unless
\code{keep_members = FALSE};
\item \code{obs} - the matching lake observations, depth-aligned to \code{depths}.
}

\code{\link[=plot_ensemble]{plot_ensemble()}} accepts the result directly, so a summary built once can
be replotted for several variables, depths and intervals for free.

\code{as.data.frame()} returns the \code{stats} table - the long per-date summary,
ready to write out or join to something else.
}
\seealso{
\code{\link[=plot_ensemble]{plot_ensemble()}}, \code{\link[=score_ensemble]{score_ensemble()}}, \code{\link[=run_aeme_ensemble]{run_aeme_ensemble()}}
}
