% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_calib_control.R
\name{create_calib_control}
\alias{create_calib_control}
\title{Create control list for calibration}
\usage{
create_calib_control(
  file_type = "db",
  file_name = NULL,
  file_dir = "calib_sa",
  na_value = 999,
  parallel = TRUE,
  ncore = parallel::detectCores() - 1,
  timeout = Inf,
  VTR = -Inf,
  NP = NA,
  itermax = 200,
  reltol = 0.07,
  cutoff = 0.25,
  mutate = 0.1,
  c_method = "CMAES",
  ...
)
}
\arguments{
\item{file_type}{Character. Output type: \code{"csv"} or \code{"db"}.
Default \code{"db"}.}

\item{file_name}{Character. Output file name. Defaults to
\code{"results.db"} (db) or \code{"simulation_metadata.csv"} (csv).}

\item{file_dir}{Character. Output directory. Default \code{"calib_sa"}.}

\item{na_value}{Numeric. Replacement for \code{NA}. Default \code{999}.}

\item{parallel}{Logical. Run in parallel? Default \code{TRUE}.}

\item{ncore}{Integer. Number of cores if \code{parallel = TRUE}.
Default \code{parallel::detectCores() - 1}.}

\item{timeout}{Numeric. Max runtime in seconds. Default \code{Inf}.}

\item{VTR}{Numeric. Target objective value. Default \code{-Inf}.}

\item{NP}{Integer. Population size. Default \code{NA}.}

\item{itermax}{Integer. Maximum iterations. Default \code{200}.}

\item{reltol}{Numeric. Relative convergence tolerance. Default \code{0.07}.}

\item{cutoff}{Numeric. Quantile cutoff (0–1).}

\item{mutate}{Numeric. Fraction of population to mutate (0–1).}

\item{c_method}{Character. \code{"CMAES"} or \code{"LHC"}. Default \code{"CMAES"}.}

\item{...}{Must be empty. Additional arguments are not allowed.}
}
\value{
A control list.
}
\description{
Create control list for calibration
}
