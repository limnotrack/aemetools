% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pest.R
\name{plot_pest_ensemble}
\alias{plot_pest_ensemble}
\title{Plot prior against posterior parameter distributions}
\usage{
plot_pest_ensemble(ctrl, param, scaled = FALSE)
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}

\item{param}{dataframe; the calibrated parameters, for bounds.}

\item{scaled}{Logical. Rescale each parameter to \code{0-1} across its
\verb{[min, max]} bounds and draw a single panel? Default \code{FALSE}. Useful
when there are many parameters with wildly different magnitudes.}
}
\value{
A ggplot object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

One panel per parameter, prior and posterior ensembles as violins on a
shared axis, with the calibration bounds marked. This is the plot that
shows which parameters the observations informed: a posterior much
narrower than the prior was constrained, one the same width was not, and
one pressed against a bound suggests the bound is doing the work rather
than the data.
}
\seealso{
\code{\link[=pest_param_summary]{pest_param_summary()}}
}
