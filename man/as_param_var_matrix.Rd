% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_param_var_matrix.R
\name{as_param_var_matrix}
\alias{as_param_var_matrix}
\title{Coerce a parameter/variable specification into a \code{param_var_matrix}}
\usage{
as_param_var_matrix(x, param, vars_sim)
}
\arguments{
\item{x}{A dataframe, named list, logical matrix or \code{NULL}. See
\emph{Accepted forms}.}

\item{param}{dataframe; as passed to \code{\link{calib_aeme}}, requiring
the columns \code{model}, \code{file}, \code{name} and (optionally) \code{group}, \code{index},
\code{name_full}.}

\item{vars_sim}{Character vector of the variables being calibrated.}
}
\value{
A dataframe with columns \code{model}, \code{file}, \code{name_full} and one
logical column per entry of \code{vars_sim}, or \code{NULL} when \code{x} is \code{NULL}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A \code{param_var_matrix} declares which parameters are allowed to be informed
by which response variables. It is used in two places:
\itemize{
\item \code{c_method = "MOEDA"} masks the joint covariance it resamples from, so
parameters that share no variable are never correlated (see
\code{\link{calib_aeme}});
\item \code{engine = "pest"} turns the same declaration into a \code{pestpp-ies}
localizer (see \code{\link{pest_localizer}}).
}

The canonical form is the dataframe that
\code{\link{create_param_var_matrix}} returns - one row per parameter, one
logical column per variable - but writing that out by hand is tedious.
This function accepts the shorthand forms as well and returns the
canonical dataframe, so both engines take either.
}
\section{Accepted forms}{


\describe{
\item{dataframe}{The canonical form: columns \code{model}, \code{file},
\code{name_full} and one logical column per variable. Passed through with
validation only.}
\item{named list}{Keyed by variable, each element a character vector of
\emph{selectors}. A selector matches a full parameter name
(\code{"light/Kw[1]"}), a parameter group (\code{"light"}) or a bare parameter
name (\code{"Kw"}); \code{TRUE} or \code{"all"} selects every parameter and
\code{character(0)}, \code{FALSE} or \code{NULL} selects none. A selector that
matches nothing is an error rather than a silent omission.}
\item{logical matrix}{Parameters as rows (row names are \code{name_full}, or
\code{param} order when unnamed), variables as columns.}
}
}

\section{Defaults for what you leave out}{


A \strong{variable} that the specification does not mention is left
\emph{unrestricted}: every parameter is linked to it. A partial specification
therefore only tightens what it names, and forgetting a variable cannot
silently remove parameters from the problem.

A \strong{parameter} that no variable selects is linked to nothing.
\code{\link{calib_aeme}} warns about those and drops them from the
calibration, which is the intended reading of "this parameter drives
nothing I am fitting" - and, on the PEST path, avoids handing
\code{pestpp-ies} a parameter it would treat as fixed without saying so.
}

\examples{
param <- data.frame(model = "glm_aed", file = "glm4.nml",
                    group = c("light", "light", NA),
                    name = c("Kw", "ce", "MET_tmpair"),
                    index = c(1, NA, NA))
# Temperature is driven by the light and meteorological parameters;
# oxygen only by Kw. Any other variable would be left unrestricted.
as_param_var_matrix(list(HYD_temp = c("light", "MET_tmpair"),
                         CHM_oxy = "Kw"),
                    param = param,
                    vars_sim = c("HYD_temp", "CHM_oxy"))
}
\seealso{
\code{\link[=create_param_var_matrix]{create_param_var_matrix()}}, \code{\link[=pest_localizer]{pest_localizer()}}, \code{\link[=calib_aeme]{calib_aeme()}}
}
