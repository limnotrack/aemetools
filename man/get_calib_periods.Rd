% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_calib_periods.R
\name{get_calib_periods}
\alias{get_calib_periods}
\title{Suggest calibration and validation periods from observation availability}
\usage{
get_calib_periods(
  aeme,
  vars_sim = NULL,
  frac = 0.7,
  min_obs = 1L,
  weight_by = c("obs", "dates"),
  split = TRUE,
  min_density = 0.25,
  density_by = c("profiles", "obs", "dates"),
  min_depths = 3L,
  min_years = 4L
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{vars_sim}{Character. Variables (\code{var_aeme} values) that must be
represented in both periods, e.g. \code{c("HYD_temp", "CHM_oxy")}. Default
\code{NULL} uses every variable present in the observations.}

\item{frac}{Numeric between 0 and 1. Target proportion of the record to
place in the calibration period. Default \code{0.7}. Ignored when
\code{split = FALSE}.}

\item{min_obs}{Integer. Minimum observations of \emph{each} variable in
\emph{each} period for a split to be considered. Default \code{1}. Ignored when
\code{split = FALSE}.}

\item{weight_by}{Character. What \code{frac} is a proportion of: \code{"obs"}
(individual observations, so a deep profile counts more than a surface
sample) or \code{"dates"} (distinct sampling days, so every visit counts
once). Default \code{"obs"}. Ignored when \code{split = FALSE}.}

\item{split}{Logical. \code{TRUE} (default) splits the record into
calibration and validation periods. \code{FALSE} returns the whole record as
one period - see \emph{Conditioning on the whole record}.}

\item{min_density}{Numeric. Drop leading and trailing calendar years
holding fewer than this fraction of the median year's measure, so the
simulation does not run over a sparse tail. Default \code{0.25}; \code{0} keeps
the record whole. Ignored on records spanning fewer than \code{min_years}
complete years, where a median across years means little.}

\item{density_by}{Character. What \code{min_density} measures a year by:
\code{"profiles"} (default) counts casts carrying at least \code{min_depths}
distinct depths, \code{"obs"} counts individual readings, \code{"dates"} counts
sampling days. Profiles are the default because they are what a
depth-resolved calibration is actually constrained by - a year of
surface-and-bottom pairs scores near zero on \code{"profiles"} while looking
respectable on \code{"dates"}.}

\item{min_depths}{Integer. Distinct depths a cast needs before it counts
as a profile under \code{density_by = "profiles"}. Default \code{3}. A variable
with no depths at all - water level - is a scalar series, so each of
its dates always counts.}

\item{min_years}{Integer. Fewest calendar years a record must span before
\code{min_density} is applied. Default \code{4}.}
}
\value{
An object of class \code{aeme_calib_periods}: a list with
\describe{
\item{\code{periods}}{dataframe, one row per period - \code{calib} and \code{valid},
or a single \code{all} when \code{split = FALSE} - with \code{start}, \code{stop},
\code{n_days}, \code{n_obs}, \code{n_dates} and \code{n_months} (distinct calendar
months, i.e. seasonal coverage).}
\item{\code{coverage}}{dataframe, one row per variable per period, with
\code{n_obs}, \code{n_dates}, \code{first} and \code{last}.}
\item{\code{split_date}}{Date on which the validation period starts, or
\code{NULL} when \code{split = FALSE}.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Picks a single chronological split of an \code{Aeme} object's observation
record into a calibration period (earlier) and a validation period
(later), choosing the split date that comes closest to putting \code{frac} of
the observations in the calibration period while leaving every variable
in \code{vars_sim} represented in both.

The candidate split dates are the observation dates themselves, so each
period begins and ends on a date that actually carries data - no leading
or trailing stretch of simulation with nothing to compare against.

Sparse years at either end are dropped from the \emph{period}, not from the
data: a long thin tail costs model runtime out of all proportion to what
it constrains. On a record sampled eight times a year until the early
1990s and hundreds of times a year since, starting the simulation at the
first observation buys five extra model-years for a few dozen readings.
\code{min_density} sets where that trade stops being worth it. Only the ends
are trimmed, so a lean year in the middle of a dense record is kept and
each period stays contiguous.

This reports a split rather than applying it. Pass the result to
\code{\link{set_calib_period}} to build the two runs, keeping spin-up on
the validation run - its period starts on an observation date, so without
spin-up the first comparison would be made against a model that has not
yet adjusted from its initial condition.
}
\section{Conditioning on the whole record}{
\code{split = FALSE} returns a single period spanning every observation,
instead of holding any back. Withholding data withholds information that
would otherwise have reduced predictive uncertainty, and on a sparse
record - a few years of monthly profiles - a 30\% holdout is too small to
conclude anything from, while the calibration it degrades is real. The
returned object has the same shape, so \code{\link{set_calib_period}} and
the rest of the workflow are unchanged; only its single period is named
\code{"all"} and \code{split_date} is \code{NULL}.

This is not a licence to skip checking for over-fitting - it moves that
check from a held-out period onto the posterior ensemble. Pair it with an
ensemble method (\code{create_pest_control(exe = "pestpp-ies")}) and read the
spread, together with \code{\link{pest_prior_data_conflict}} and
\code{\link{read_pest_phi_group}}. Conditioning on everything and then
reporting a single best parameter set removes the only guard and puts
nothing in its place.
}

\examples{
\dontrun{
aeme <- readRDS(system.file("extdata/aeme.rds", package = "AEME"))
p <- get_calib_periods(aeme, vars_sim = "HYD_temp")
p

calib <- set_calib_period(aeme, p, "calib")

# Condition on everything instead, and lean on the posterior ensemble.
p_all <- get_calib_periods(aeme, vars_sim = "HYD_temp", split = FALSE)
aeme_all <- set_calib_period(aeme, p_all)
}
}
\seealso{
\code{\link{set_calib_period}}, \code{\link{validate_aeme}},
\code{\link{calib_aeme}}
}
