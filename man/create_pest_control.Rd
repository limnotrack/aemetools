% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_pest_control.R
\name{create_pest_control}
\alias{create_pest_control}
\title{Create control list for PEST++ calibration}
\usage{
create_pest_control(
  file_type = "db",
  file_name = NULL,
  file_dir = "calib_sa",
  na_value = 999,
  parallel = TRUE,
  ncore = parallel::detectCores() - 1,
  timeout = Inf,
  exe = "pestpp-ies",
  obj_mode = "residual",
  noptmax = 6,
  ies_num_reals = 50,
  pest_dir = "pest",
  case = "aeme",
  port = 4004,
  solver_timeout = 24 * 3600,
  stall_minutes = 10,
  derinc = 0.01,
  derinc_lb = 1e-06,
  prior_cov = NULL,
  prior_dist = "uniform",
  prior_par_ensemble = NULL,
  seed = NULL,
  noise_sd = NULL,
  noise_method = "ensemble",
  ies_no_noise = FALSE,
  include_base = TRUE,
  restart_from = NULL,
  localizer = NULL,
  pestpp_options = list(),
  overwrite = TRUE,
  keep_files = TRUE,
  trim_output = TRUE,
  ...
)
}
\arguments{
\item{file_type}{Character. Output type: \code{"csv"} or \code{"db"}.
Default \code{"db"}.}

\item{file_name}{Character. Output file name. Defaults to
\code{"results.db"} (db) or \code{"simulation_metadata.csv"} (csv).}

\item{file_dir}{Character. Output directory. Default \code{"calib_sa"}.}

\item{na_value}{Numeric. Penalty value substituted for \code{NA} fit values
during optimisation to discourage parameter sets that produce invalid
model output. Default \code{999}.}

\item{parallel}{Logical. Run the model evaluations in parallel? Default
\code{TRUE}. PEST++ parallelises only through a run manager, so this selects
the PANTHER manager (a master plus \code{ncore} agents) rather than a serial
run in which the master evaluates the model itself. aemetools does
\strong{not} create its own \code{parallel::makeCluster()} for a PEST++ run;
\code{ncore} is the number of PANTHER agents.}

\item{ncore}{Integer. Number of cores if \code{parallel = TRUE}.
Default \code{parallel::detectCores() - 1}.}

\item{timeout}{Numeric. Max runtime in seconds. Default \code{Inf}.}

\item{exe}{Character. Which PEST++ solver to run. One of \code{"pestpp-ies"}
(iterative ensemble smoother; the recommended default for AEME, since it
tolerates noisy model responses and returns a posterior parameter
ensemble), \code{"pestpp-glm"} (Gauss-Levenberg-Marquardt), \code{"pestpp-sen"}
(Morris/Sobol global sensitivity) or \code{"pestpp-swp"} (parallel sweep of a
supplied parameter table).}

\item{obj_mode}{Character. How the objective function is expressed in the
\code{.pst} file.
\itemize{
\item \code{"residual"} (default) writes one PEST observation per
observed value, so PEST++ minimises the usual weighted sum of squared
residuals. This is the idiomatic PEST formulation and is what enables
regularisation, FOSM uncertainty and per-observation diagnostics -
but it \strong{ignores \code{FUN_list}}.
\item \code{"fit"} writes one PEST observation per variable in \code{vars_sim}, whose
"simulated" value is the output of the corresponding \code{FUN_list}
function and whose observed value is \code{0}. This preserves your existing
NSE/MAE objectives exactly, at the cost of losing residual-level
diagnostics and making the finite-difference Jacobian much less
informative (so pair it with \code{"pestpp-ies"}, not \code{"pestpp-glm"}).
}}

\item{noptmax}{Integer. Maximum number of solver iterations. Special
values follow the PEST++ convention: \code{0} runs the model once at the
initial parameter values, \code{-1} computes a Jacobian and FOSM uncertainty
without updating parameters, \code{-2} runs the prior parameter ensemble
only. Default \code{6}, a common starting point for \code{pestpp-ies}.}

\item{ies_num_reals}{Integer. Ensemble size for \code{"pestpp-ies"}. Default
\code{50}. Unlike the built-in methods, run count scales with this rather
than with the number of parameters.}

\item{pest_dir}{Character. Working directory for the generated \code{.pst},
\code{.tpl}, \code{.ins} and forward-run files. Defaults to \code{"pest"} under the
lake directory. This directory is created and, if \code{overwrite = TRUE},
cleared on each run.}

\item{case}{Character. Basename for the generated files, so the control
file is \verb{<case>.pst}. Default \code{"aeme"}.}

\item{port}{Integer. Preferred TCP port for the PANTHER master. Default
\code{4004}. This is the starting point of a search, not a fixed binding: at
launch the run takes this port if it is free and otherwise steps to the
next free one, so concurrent runs (per lake, per stage) and agents
orphaned by an aborted run do not collide on it.}

\item{solver_timeout}{Numeric. Maximum seconds to wait for the solver to
finish before aborting. Default \code{86400} (24 hours). This is a backstop
against a detached PANTHER master that neither completes nor reports an
error; it is not a per-model-run limit, which is \code{timeout}.}

\item{stall_minutes}{Numeric. Abort if the solver goes this many minutes
without completing a model run or writing to its record file. Default
\code{10}. This is the practical guard: PEST++ can finish every run,
terminate its agents and then fail to exit, spinning at 100\% CPU with
its output unflushed, which \code{solver_timeout} alone would not catch for
a further day. If every expected run is already logged the results are
kept and the run continues; otherwise it aborts. Raise this if a single
model evaluation legitimately takes longer than 10 minutes.}

\item{derinc, derinc_lb}{Numeric. Finite-difference increment (as a
fraction of the parameter value) and its absolute lower bound, used by
\code{"pestpp-glm"} and \code{"pestpp-sen"}. Defaults \code{0.01} and \code{1e-6}.}

\item{prior_cov}{Optional matrix or character. A prior parameter
covariance matrix (from \code{\link{pest_prior_cov}}) or the path to a
PEST \code{.cov}/\code{.unc} file. A matrix is used to draw a correlated Gaussian
prior ensemble in R (implies \code{prior_dist = "normal"}); a path is passed
to \code{pestpp-ies} as \code{++parcov(...)}. When \code{NULL} (the default) the prior
is independent uniforms on the bounds.}

\item{prior_dist}{Character. Shape of the prior parameter ensemble when it
is generated in R: \code{"uniform"} (default, and the only case where the
ensemble is left for \code{pestpp-ies} to draw), \code{"normal"} (Gaussian about
the initial values) or \code{"triangular"}. See
\code{\link{pest_prior_ensemble}}.}

\item{prior_par_ensemble}{Logical, character, \code{NULL}, or a previous run.
Controls the \code{++ies_parameter_ensemble}. \code{NULL} (default) generates one
in R only when \code{seed}, \code{prior_cov} or a non-uniform \code{prior_dist} /
\code{noise_sd} makes it necessary, and otherwise leaves \code{pestpp-ies} to draw
its own. \code{TRUE}/\code{FALSE} force or suppress generation; a \strong{file} path
uses that CSV as-is.

A \strong{finished \code{pestpp-ies} run} - a \code{\link{read_calib}} object, a
\code{create_pest_control()} object, or its run-directory path - seeds this
run's prior from that run's posterior ensemble. Parameters shared by
\code{name_full} take their columns from the source posterior (carrying its
marginals \emph{and} correlations); parameters new to this run are drawn from
their own prior. This is the natural way to chain the stages of a staged
calibration - stage N+1 starts from stage N's posterior rather than from
a re-widened box of independent bounds. Unlike \code{restart_from} (which
resumes the \emph{same} problem and requires an unchanged parameter and
observation set), the parameter set here may grow or shrink between
stages. \code{restart_from} still takes precedence if both are given.}

\item{seed}{Integer or \code{NULL}. Random seed for the prior parameter and
observation ensembles, for reproducible runs. Setting it triggers
in-R generation of the parameter ensemble.}

\item{noise_sd}{Named numeric vector or \code{NULL}. Measurement standard
deviation per \code{vars_sim} variable, in data units, e.g.
\code{c(HYD_temp = 0.5)}. When given, an explicit observation (noise)
ensemble is written and passed as \code{++ies_observation_ensemble}, so the
noise is decoupled from the balanced PEST weights. See
\code{\link{pest_obs_ensemble}}.}

\item{noise_method}{Character. How \code{noise_sd} is applied. \code{"ensemble"}
(default) draws an explicit observation ensemble in R and passes it as
\code{++ies_observation_ensemble}, so realisations align with a generated
parameter ensemble. \code{"standard_deviation"} instead writes the
per-variable standard deviations as a diagonal observation-noise
covariance (\code{++obscov}) and lets \code{pestpp-ies} draw the noise itself.
\code{"none"} ignores \code{noise_sd}.}

\item{ies_no_noise}{Logical. Emit \code{++ies_no_noise(true)}, so \code{pestpp-ies}
fits the raw observed values with no measurement-noise ensemble.
Default \code{FALSE}.}

\item{include_base}{Logical. Keep the initial parameter values as a
\code{base} realisation in the ensemble (\code{++ies_include_base}). Default
\code{TRUE}, matching \code{pestpp-ies}.}

\item{restart_from}{Optional. A previous \code{pestpp-ies} run to resume from -
a run directory, a \code{\link{read_calib}} object, or a
\code{\link{create_pest_control}} object. Its final parameter and
simulated-observation ensembles are handed to the new run as
\code{++ies_parameter_ensemble} and \code{++ies_restart_observation_ensemble}, so
iteration 0 is not re-evaluated. The parameter and observation sets must
be unchanged from that run. Takes precedence over \code{prior_par_ensemble}.}

\item{localizer}{Optional. A \code{pestpp-ies} localizer, declaring which
parameters each variable's observations may update. Accepts anything
\code{\link{as_param_var_matrix}} takes - most conveniently a list keyed
by variable, e.g.
\code{list(HYD_temp = c("light", "mixing"), CHM_oxy = "sediment")} - or the
path to a localizer file already in PEST matrix format, which is passed
through unchanged. A variable the specification does not mention is left
linked to every parameter. \code{NULL} (the default) runs without
localization. Only \code{"pestpp-ies"} localizes; see
\code{\link{pest_localizer}} for what the entries mean and for the
cost of a localized upgrade.}

\item{pestpp_options}{Named list. Additional \verb{++} options written verbatim
to the control file, e.g.
\code{list(ies_bad_phi_sigma = 2.0, ies_autoadaloc = TRUE)}. Names are
emitted as \code{++name(value)}, and override the defaults aemetools sets.

\code{ies_drop_conflicts} is one worth knowing about. It is \strong{off} by
default: \code{pestpp-ies} runs with no observation noise unless an
obs-noise-specific option is given, and against noiseless observations
the prior-data-conflict test flags nearly everything - dropping which
can leave nothing to fit, aborting the run. Conflicts are still
detected and readable with \code{\link{pest_prior_data_conflict}}. Turn
it on once \code{noise_sd} makes the test meaningful.}

\item{overwrite}{Logical. Clear \code{pest_dir} before writing? Default \code{TRUE}.}

\item{keep_files}{Logical. Retain the PEST working directory after the run
completes, for inspection or restart. Default \code{TRUE}.}

\item{trim_output}{Logical. Before staging the model for PEST++, restrict
each model's written output to \code{vars_sim} via
\code{\link[AEME]{set_output_vars}} (and switch off GLM's
\code{mass_balance.csv}). Default \code{TRUE}. Rewrites the model configuration in
the lake directory; the trimmed config is what gets copied to the PANTHER
agent directories.}

\item{...}{Must be empty. Additional arguments are not allowed.}
}
\value{
A \code{calib_sa_control} list with \code{method = "pest"}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Builds the control object used when calibrating with an external PEST++
engine instead of the built-in \code{"CMAES"}/\code{"LHC"}/\code{"MOEDA"} methods. The
returned object is a \code{calib_sa_control} like those from
\code{\link{create_calib_control}}, so the existing result-writing and
plotting functions keep working; the extra fields describe how the
\code{.pst} control file is written and how the solver is invoked.
}
\examples{
ctrl <- create_pest_control(exe = "pestpp-ies", noptmax = 6,
                            ies_num_reals = 50, ncore = 4)
ctrl
}
\seealso{
\code{\link[=install_pest]{install_pest()}}, \code{\link[=write_pst]{write_pst()}}, \code{\link[=calib_aeme]{calib_aeme()}}
}
