% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pest_diagnostics.R
\name{pest_prior_data_conflict}
\alias{pest_prior_data_conflict}
\title{Read the \code{pestpp-ies} prior-data-conflict report}
\usage{
pest_prior_data_conflict(ctrl)
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}
}
\value{
A dataframe: the columns of \verb{<case>.pdc.csv} (observation name
plus the solver's per-observation statistics), left-joined to
\code{var_aeme}, \code{Date} and \code{depth} from the observation map. A zero-row
dataframe when no conflict file was written.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{pestpp-ies} flags an observation as being in \emph{prior-data conflict} when
the prior (iteration 0) simulated ensemble cannot bracket the observed
value: no combination of parameters within their prior ranges reproduces
it. A conflicted observation is usually a modelling finding ("the prior
cannot reach the observed hypolimnetic temperatures"), not a nuisance,
which is why aemetools leaves \code{ies_drop_conflicts} off by default and
reports the conflicts here instead. Turn it on through \code{pestpp_options}
to have \code{pestpp-ies} remove them from the update - but only once
\code{noise_sd} is set, or the conflict test is comparing against observed
values with no spread and will flag nearly everything. This reader is
then the only way to see which observations went.

Reads \verb{<case>.pdc.csv}, which \code{pestpp-ies} writes only when at least one
conflict is detected.
}
\seealso{
\code{\link[=read_pest_phi]{read_pest_phi()}}, \code{\link[=pest_residuals]{pest_residuals()}}
}
