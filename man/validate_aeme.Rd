% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_aeme.R
\name{validate_aeme}
\alias{validate_aeme}
\title{Score a calibrated parameter set over the calibration and validation
periods}
\usage{
validate_aeme(
  aeme,
  param,
  periods,
  model,
  vars_sim,
  path,
  FUN_list,
  weights,
  model_controls = NULL,
  spin_up = NULL,
  na_value = 999
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{param}{dataframe; the calibrated parameters, as returned by
\code{\link{update_param}} or \code{\link{get_best_params}}.}

\item{periods}{An \code{aeme_calib_periods} object from
\code{\link{get_calib_periods}}.}

\item{model}{string; for which model. Options are c("dy_cd", "glm_aed",
"gotm_wet", "simstrat_aed", "simstrat_aed2").}

\item{vars_sim}{vector; of variable names used in the calculation of model
fit.}

\item{path}{character; directory where input files are located. Defaults to
the path stored in \code{aeme}, or the current working directory if not set.}

\item{FUN_list}{function; of the form \verb{function(df)} using \code{df$model} and
\code{df$obs}, used to calculate model fit. If NULL, uses mean absolute error
(MAE).}

\item{weights}{vector; of weights to be used in the calculation of model fit.}

\item{model_controls}{data.frame; model configuration, typically loaded via
\code{\link[AEME:get_model_controls]{get_model_controls()}}.}

\item{spin_up}{Numeric or \code{NULL}. Passed to
\code{\link{set_calib_period}}.}

\item{na_value}{numeric; value to be returned if the model fails to run.}
}
\value{
A list of class \code{aeme_validation}:
\describe{
\item{\code{fit}}{dataframe with one row per variable per period: \code{period},
\code{var_aeme}, \code{n_obs} and \code{fit}.}
\item{\code{degradation}}{dataframe with one row per variable: \code{calib},
\code{valid} and \code{degradation} (\code{valid - calib}; positive means the
held-out period fits worse).}
\item{\code{comparison}}{the modelled/observed rows behind the fits, with
a \code{period} column.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The split-sample half of the PEST workflow. Runs \code{model} once per period
from \code{\link{get_calib_periods}}, both times with the \emph{same}
(calibrated) parameters, and reports the fit of each variable in each
period.

The comparison is the point: a parameter set that fits the calibration
period well and the held-out period much worse has been fitted to noise
or to conditions that do not recur, which is exactly what a \code{pestpp-ies}
run driven to a low phi can produce. \code{degradation} reports that gap.

Fit values come from \code{FUN_list}, so they carry the same orientation as in
calibration - the package's fitness functions are all written so that
\emph{lower is better} (see \code{\link{nse}} and friends, which return the
negated statistic). A positive \code{degradation} therefore always means the
validation period fits worse.
}
\examples{
\dontrun{
p <- get_calib_periods(aeme, vars_sim = "HYD_temp")
sim_id <- calib_aeme(aeme = set_calib_period(aeme, p, "calib"),
                     param = param, model = "glm_aed",
                     vars_sim = "HYD_temp",
                     ctrl = create_pest_control(exe = "pestpp-ies"))
calib <- read_pest_results(ctrl)
best <- update_param(calib = calib, param = param)

v <- validate_aeme(aeme = aeme, param = best, periods = p,
                   model = "glm_aed", vars_sim = "HYD_temp",
                   FUN_list = list(HYD_temp = kge))
v$degradation
}
}
\seealso{
\code{\link{get_calib_periods}}, \code{\link{set_calib_period}},
\code{\link{calib_aeme}}
}
