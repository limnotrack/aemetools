% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calib_aeme_pest.R
\name{read_pest_results}
\alias{read_pest_results}
\title{Import PEST++ model runs into the aemetools results shape}
\usage{
read_pest_results(ctrl, param, vars_sim)
}
\arguments{
\item{ctrl}{list; from \code{\link{create_pest_control}}.}

\item{param}{dataframe; the parameters that were calibrated.}

\item{vars_sim}{character vector; simulated variables.}
}
\value{
A dataframe of model runs, or a zero-row dataframe if none ran.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reads the run log written by \code{\link{pest_forward_run}} and returns a
dataframe in the shape \code{\link{calib_aeme}} produces: one row per
model evaluation, one column per \code{param$name_full}, one column per
\code{vars_sim} entry, plus \code{fit} and \code{gen}.

The run log is used in preference to PEST++'s own output files because
those differ between solvers (\code{pestpp-ies} writes parameter and
observation ensembles, \code{pestpp-glm} writes a single best-parameter file)
and their names have changed between releases. The log also captures
\emph{every} evaluation, including rejected ones, which is what the results
schema records.

Generation numbers are recovered by matching logged parameter vectors
against the per-iteration ensembles \verb{<case>.<n>.par.csv} when the solver
writes them; otherwise every run is reported as generation 1.
}
