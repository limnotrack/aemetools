% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_aeme.R
\name{set_calib_period}
\alias{set_calib_period}
\title{Apply a calibration or validation period to an Aeme object}
\usage{
set_calib_period(aeme, periods, period = NULL, spin_up = NULL)
}
\arguments{
\item{aeme}{Aeme object.}

\item{periods}{An \code{aeme_calib_periods} object from
\code{\link{get_calib_periods}}.}

\item{period}{Character or \code{NULL}. Which period to apply - \code{"calib"} or
\code{"valid"} from a split, or \code{"all"} from \code{split = FALSE}. \code{NULL}
(default) takes the object's first period, so a no-split object needs
no argument at all.}

\item{spin_up}{Numeric or \code{NULL}. Days of spin-up to set. \code{NULL}
(default) keeps whatever the object already carries.}
}
\value{
\code{aeme}, with its simulation window set to the chosen period.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Sets the simulation window from one row of a
\code{\link{get_calib_periods}} result, so the split feeds straight into
\code{\link{calib_aeme}} - including the PEST++ engines, which take their
observation window from \code{AEME::time(aeme)} by way of
\code{\link{pest_obs_table}}.

Spin-up is carried over from the object unless \code{spin_up} is given. Both
periods start on an observation date, so the validation run needs spin-up
just as much as the calibration run does: without it the first residual
is measured against a model that has not yet moved off its initial
condition.
}
\examples{
\dontrun{
p <- get_calib_periods(aeme, vars_sim = "HYD_temp")
aeme_cal <- set_calib_period(aeme, p, "calib")
sim_id <- calib_aeme(aeme = aeme_cal, param = param, model = "glm_aed",
                     vars_sim = "HYD_temp",
                     ctrl = create_pest_control(exe = "pestpp-ies"))
}
}
\seealso{
\code{\link{get_calib_periods}}, \code{\link{validate_aeme}}
}
