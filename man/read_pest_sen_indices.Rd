% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_pest_sen.R
\name{read_pest_sen_indices}
\alias{read_pest_sen_indices}
\title{Read \code{pestpp-sen} Method of Morris sensitivity indices}
\usage{
read_pest_sen_indices(ctrl, param, vars_sim = NULL, model = NULL)
}
\arguments{
\item{ctrl}{A \code{\link{create_sen_control}} object with its \code{pest_dir}
resolved (as inside \code{\link{sa_aeme}}), or a path to the run
directory.}

\item{param}{dataframe; the parameters that were analysed.}

\item{vars_sim}{character; unused, kept for signature symmetry with the
other readers.}

\item{model}{character; model name, used only to abbreviate parameter
labels.}
}
\value{
A long dataframe with columns \code{model}, \code{variable}, \code{parameter},
\code{label}, \code{index_type} (\code{"mu_star"}, \code{"mu"}, \code{"sigma"}, and
\code{"scaled_sen"} when \verb{<case>.mos} is present), \code{value}, \code{low_ci},
\code{high_ci}. Zero rows (with a warning) when no recognised file is found.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Parses the Morris sensitivity summary written by \code{pestpp-sen} into a tidy
dataframe, translating the synthetic PEST parameter names (\code{p001}, ...)
back to aemetools \code{name_full} identities and the observation groups back
to the \code{names(ctrl$vars_sim)} sub-regions.

\code{pyemu} ships no parser for these files. \code{pestpp-sen} with
\code{gsa_morris_obs_sen} writes several: \verb{<case>.group.msn} (per observation
group), \verb{<case>.mio} (per observation), \verb{<case>.mos} (per observation,
including a range-scaled effect), and \verb{<case>.msn} (pooled over all
observations). The most disaggregated one available is used, in that
order, falling back to the pooled file with \code{variable = "all"}. Columns
are matched on name patterns rather than fixed positions.

Raw elementary effects have the units of the response divided by the
parameter's units, so a parameter with a wide \verb{[min, max]} produces a
\code{mu_star} orders of magnitude larger than a tightly-bounded one. When
\verb{<case>.mos} is present its \code{scaled_sen} column (effect scaled by the
parameter range) is also returned, as \code{index_type = "scaled_sen"}, and is
the better basis for ranking across parameters.
}
\seealso{
\code{\link{read_sen}}, \code{\link{plot_sen}},
\code{\link{create_sen_control}}
}
