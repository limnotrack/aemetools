% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pst.R
\name{pest_param_table}
\alias{pest_param_table}
\title{Build the PEST parameter table from an aemetools \code{param} dataframe}
\usage{
pest_param_table(param, transform = TRUE)
}
\arguments{
\item{param}{dataframe; as passed to \code{\link{calib_aeme}}, requiring
the columns \code{model}, \code{file}, \code{group}, \code{name}, \code{index}, \code{value}, \code{min},
\code{max} and optionally \code{log}.}

\item{transform}{Logical. Honour \code{param$log} by writing \code{partrans = "log"}
for strictly-positive parameters? Default \code{TRUE}. Set \code{FALSE} for a
sensitivity analysis, where every parameter must be sampled linearly on
\verb{[min, max]} so the indices are comparable with a built-in
\code{\link{sa_aeme}} run.}
}
\value{
A dataframe with columns \code{parnme}, \code{partrans}, \code{parchglim},
\code{parval1}, \code{parlbnd}, \code{parubnd}, \code{pargp}, \code{scale}, \code{offset}, \code{dercom},
carrying a \code{map} attribute (a dataframe of \code{parnme}/\code{name_full}/\code{model}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Translates the aemetools parameter dataframe into the columns of the
\verb{* parameter data} section of a PEST control file, and assigns each
parameter a short, safe PEST name.

aemetools names parameters as \code{group/name[index]} (see
\code{\link{encode_param}}), which contains characters (\code{/}, \code{[}, \verb{]})
that collide with PEST template-file delimiters and with the fixed-width
name conventions of the older PEST utilities. Rather than escaping them,
each parameter is given a synthetic name \code{p001}, \code{p002}, ... and the
mapping back to \code{name_full} is returned as the \code{map} attribute so results
can be translated back afterwards.
}
