% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pest.R
\name{plot_pest_phi}
\alias{plot_pest_phi}
\title{Plot the PEST++ objective-function trajectory}
\usage{
plot_pest_phi(ctrl, type = "actual", log_y = TRUE)
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}

\item{type}{Character. Which phi to read. \code{"actual"} (default) is
measured against the observed values; \code{"meas"} is against the noise
realisations each ensemble member was assigned; \code{"composite"} adds the
regularisation term when \code{ies_reg_factor} is non-zero.}

\item{log_y}{Logical. Log-scale the phi axis? Default \code{TRUE}, since phi
typically falls by orders of magnitude in the first iteration and a
linear axis then hides everything that follows.}
}
\value{
A ggplot object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Phi against iteration, with the ensemble's min-max range shaded and the
mean and best member drawn on top. A trajectory that flattens says
further iterations are not improving the fit; one where the band stays
wide says the ensemble has not converged even if the best member has.
}
\seealso{
\code{\link[=read_pest_phi]{read_pest_phi()}}, \code{\link[=plot_pest_ensemble]{plot_pest_ensemble()}}
}
