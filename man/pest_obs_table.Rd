% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pst.R
\name{pest_obs_table}
\alias{pest_obs_table}
\title{Build the PEST observation table from an AEME object}
\usage{
pest_obs_table(
  aeme,
  vars_sim,
  weights,
  obj_mode = "residual",
  weight_method = "balanced",
  var_indices = NULL
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{vars_sim}{vector; of variables names to be used in the calculation of
model fit.}

\item{weights}{a named vector; of weights for each variable in vars_sim. If not
provided, defaults to 1 for each variable.}

\item{obj_mode}{Character. \code{"residual"} writes one PEST observation per
observed value; \code{"fit"} writes a single observation per variable whose
simulated value is the \code{FUN_list} output and whose observed value is
zero. See \code{\link{create_pest_control}}.}

\item{weight_method}{Character. \code{"balanced"} (default) scales each
observation's weight by \code{sqrt(weights[v] / n_v) / sd_v}, so that each
variable contributes roughly \code{weights[v]} to the initial objective
function regardless of how many observations it has or what units it is
in - the closest PEST analogue of the per-variable \code{weights} used by
\code{\link{run_and_fit}}. \code{"unit"} uses \code{weights[v]} directly for every
observation of that variable.}

\item{var_indices}{list; from \code{run_and_fit(return_indices = TRUE)}. When
supplied, observations are restricted to the dates the model actually
wrote. Otherwise the simulation window from \code{AEME::time()} is used.
Observations the model cannot simulate must not become PEST
observations: they carry weight, so they would contribute to phi while
never being reproducible.}
}
\value{
A dataframe with columns \code{obsnme}, \code{obsval}, \code{weight}, \code{obgnme},
carrying a \code{map} attribute linking each \code{obsnme} back to its
\code{Date}/\code{depth}/\code{var_aeme}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
