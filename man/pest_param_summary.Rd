% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_pest_output.R
\name{pest_param_summary}
\alias{pest_param_summary}
\title{Compare prior and posterior parameter distributions}
\usage{
pest_param_summary(ctrl, param)
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}

\item{param}{dataframe; the calibrated parameters, for bounds.}
}
\value{
A dataframe, one row per parameter, ordered by decreasing
variance reduction.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The question a calibration run is really answering is not "what is the
best value" but "which parameters did the data actually constrain".
This summarises each parameter's prior (iteration 0) and posterior
(final iteration) ensembles side by side and reports the \strong{variance
reduction}, \code{1 - sd_post / sd_prior}.

A value near 1 means the observations pinned that parameter down; near 0
means the posterior is as wide as the prior and the data said nothing
about it - a parameter you could fix, or need different observations to
inform. Negative values (posterior wider than prior) usually indicate a
poorly posed problem.

\code{bound_frac} reports the posterior spread as a fraction of the
parameter's \verb{[min, max]} range, so a parameter whose posterior is
pressed against a bound is visible. \code{post_base} is the posterior value of
the \code{base} realisation - where the initial (often hand-calibrated)
parameter set ended up - or \code{NA} when the run carried no base.
}
\seealso{
\code{\link[=plot_pest_ensemble]{plot_pest_ensemble()}}
}
