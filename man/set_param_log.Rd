% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_param_log.R
\name{set_param_log}
\alias{set_param_log}
\title{Flag parameters for log transformation in a PEST calibration}
\usage{
set_param_log(param, ratio = 10, overwrite = TRUE)
}
\arguments{
\item{param}{dataframe; as passed to \code{\link{calib_aeme}}, requiring
at least the \code{min} and \code{max} columns.}

\item{ratio}{Numeric \verb{>= 1}. Minimum \code{max / min} for a parameter to be
flagged. \code{Inf} flags nothing. Default \code{10}.}

\item{overwrite}{Logical. Replace an existing \code{log} column outright?
Default \code{TRUE}. When \code{FALSE}, rows already \code{TRUE} are kept and only the
remaining rows are set from the rule.}
}
\value{
\code{param} with a logical \code{log} column.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Adds (or overwrites) the logical \code{log} column that
\code{\link{pest_param_table}} and \code{\link{pest_prior_cov}} read to
decide which parameters \code{pestpp-ies} samples in log10 space. A parameter
is flagged when it is strictly positive and its feasible range spans at
least \code{ratio} (default one order of magnitude) - the point at which a
linear prior spends most of its ensemble in the top of the range.

\code{\link{pest_param_table}} independently drops the transform for any
parameter whose lower bound is not positive, so a flag set here can never
produce an invalid control file. The positivity check is repeated only so
the column is meaningful on its own.
}
\examples{
param <- data.frame(
  model = "glm_aed", file = "glm3.nml", group = "light",
  name = c("Kw", "sed_temp_mean"), index = NA_real_,
  value = c(0.5, 8), min = c(0.05, 4), max = c(5, 12)
)
set_param_log(param)$log   # TRUE  (0.05..5 spans 100x)
                           # FALSE (4..12 spans 3x)
}
\seealso{
\code{\link[=pest_param_table]{pest_param_table()}}, \code{\link[=pest_prior_cov]{pest_prior_cov()}}, \code{\link[=freeze_param]{freeze_param()}}
}
