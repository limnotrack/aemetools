% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_pest.R
\name{install_pest}
\alias{install_pest}
\title{Install the PEST++ executables}
\usage{
install_pest(
  version = "latest",
  dir = pest_install_dir(),
  os = NULL,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{version}{Character. Release tag to install, e.g. \code{"5.2.16"}, or
\code{"latest"} (the default) to query the GitHub API for the most recent
release.}

\item{dir}{Character. Directory to install into. Defaults to
\code{tools::R_user_dir("aemetools", "data")/pestpp}, which persists between
sessions.}

\item{os}{Character. One of \code{"win"}, \code{"iwin"} (Intel-compiled Windows),
\code{"linux"} or \code{"mac"}. Defaults to the current platform.}

\item{force}{Logical. Reinstall even if the executables are already
present. Default \code{FALSE}.}

\item{quiet}{Logical. Suppress progress messages. Default \code{FALSE}.}
}
\value{
Invisibly, the directory containing the installed executables.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Downloads a PEST++ release from
\url{https://github.com/usgs/pestpp/releases} and unpacks the executables
into a per-user data directory so that \code{\link{calib_aeme}} can find
them. PEST++ is public-domain software distributed by the USGS; it is \emph{not}
bundled with aemetools and is not required unless
\code{ctrl$c_method} is one of the \code{"PESTPP-*"} methods.
}
\examples{
\dontrun{
install_pest()
pest_exe_path("pestpp-ies")
}
}
\seealso{
\code{\link[=pest_exe_path]{pest_exe_path()}}, \code{\link[=have_pest]{have_pest()}}
}
