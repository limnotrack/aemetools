% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pest.R
\name{plot_pest_timeseries}
\alias{plot_pest_timeseries}
\title{Plot the posterior simulated ensemble against observations over time}
\usage{
plot_pest_timeseries(
  calib,
  obs_tbl = NULL,
  vars = NULL,
  iteration = NULL,
  ci = c(0.05, 0.95)
)
}
\arguments{
\item{obs_tbl}{dataframe; from \code{\link{pest_obs_table}}. When
missing, the observed values are read from the control file written
beside the case.}

\item{vars}{Character or \code{NULL}. Restrict to these \code{var_aeme} values.}

\item{iteration}{Integer. Which iteration to read. \code{NULL} (the default)
reads the last one written, i.e. the posterior.}

\item{ci}{Length-2 numeric. Lower/upper quantiles for the ribbon.
Default \code{c(0.05, 0.95)}.}
}
\value{
A ggplot object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

For each variable, the ensemble's simulated range over time (a ribbon of
across-realisation quantiles with the median on top) with the observed
values as points. This is the plot that shows \emph{when} the ensemble
reproduces the observations and when it does not - seasonal drift, a
missed stratification onset - which neither the aggregate fit nor a
simulated-vs-observed scatter reveals.
}
\seealso{
\code{\link[=pest_residuals]{pest_residuals()}}, \code{\link[=plot_pest_residuals]{plot_pest_residuals()}}
}
