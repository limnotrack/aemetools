% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pest_posterior_params.R
\name{pest_posterior_params}
\alias{pest_posterior_params}
\title{Extract a PEST++ posterior parameter ensemble as runnable parameter sets}
\usage{
pest_posterior_params(
  calib,
  param = NULL,
  iteration = NULL,
  include_base = FALSE,
  n_max = NULL
)
}
\arguments{
\item{calib}{A completed calibration: the object from
\code{\link{read_calib}}, a run-directory path, or a
\code{\link{create_pest_control}} object.}

\item{param}{dataframe; optional parameter template used instead of
\code{calib$parameter_metadata} (needed when \code{calib} is a bare path). Must
carry \code{model}, \code{file}, \code{name}, \code{group}, \code{index}, \code{value}, \code{min}, \code{max}.}

\item{iteration}{Integer or \code{NULL}. Which ensemble to read; \code{NULL} (the
default) is the last (posterior), \code{0} is the prior. Passed to
\code{\link{read_pest_ensemble}}.}

\item{include_base}{Logical. Keep the \code{base} realisation (the initial
parameter values carried through the run by \code{ies_include_base})?
Default \code{FALSE}.}

\item{n_max}{Integer or \code{NULL}. Keep only the first \code{n_max} realisations,
for a quick look at a large ensemble. Default \code{NULL} (all).}
}
\value{
An object of class \code{aeme_param_sets}: a named list (names are the
realisation ids), each element a long \code{param} dataframe with columns
\code{model}, \code{file}, \code{name}, \code{group}, \code{index}, \code{value}, \code{min}, \code{max},
\code{name_full}. \code{as.data.frame()} stacks it into one long frame with an
\code{ensemble} id column - the other form \code{\link{run_aeme_ensemble}}
accepts.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A finished \code{pestpp-ies} run leaves a \strong{posterior parameter ensemble} -
one parameter vector per realisation, the product of an iterative ensemble
smoother. This turns that ensemble into a list of \code{param} dataframes, each
one directly runnable by \code{\link{run_aeme_ensemble}} (via its
\code{param_sets} argument) or \code{\link{run_aeme_param}}.

Every set is \emph{complete}: parameters that were held fixed during the run -
frozen earlier stages of a staged calibration, or anything with
\verb{value == min == max} - are carried through unchanged from the template,
because the solver's ensemble only contains the adjustable parameters.

\code{iteration = 0} returns the \strong{prior} ensemble instead, for a
prior-to-posterior predictive comparison.
}
\examples{
\dontrun{
calib <- read_calib(ctrl = cal_ctrl, sim_id = cal_id)

post <- pest_posterior_params(calib)          # runnable posterior sets
post

aeme_ens <- run_aeme_ensemble(aeme = aeme, model = "glm_aed", path = path,
                              param_sets = post, parallel = TRUE)
plot_ensemble(aeme_ens, model = "glm_aed", var_sim = "HYD_temp")

prior <- pest_posterior_params(calib, iteration = 0)   # prior band
}
}
\seealso{
\code{\link{run_aeme_ensemble}}, \code{\link{read_pest_ensemble}},
\code{\link{pest_param_summary}}
}
