% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pest_files.R
\name{write_pest_forward_run}
\alias{write_pest_forward_run}
\title{Write the PEST forward-run script and its payload}
\usage{
write_pest_forward_run(
  aeme,
  param,
  par_tbl,
  obs_tbl,
  model,
  vars_sim,
  FUN_list,
  weights,
  ctrl,
  model_controls = NULL,
  var_indices = list(),
  include_wlev = FALSE
)
}
\arguments{
\item{aeme}{Aeme object.}

\item{param}{dataframe; of parameters read in from a csv file. Requires the
columns c("model", "file", "group", "name", "index", "value", "min", "max", "log")}

\item{par_tbl, obs_tbl}{dataframes; from \code{\link{pest_param_table}} and
\code{\link{pest_obs_table}}, carrying their \code{map} attributes.}

\item{model}{character vector; models to use. One or more of \code{"dy_cd"},
\code{"glm_aed"}, \code{"gotm_wet"}, \code{"simstrat_aed2"}. Defaults to all models if
not found in \code{aeme}.}

\item{vars_sim}{vector; of variables names to be used in the calculation of
model fit.}

\item{FUN_list}{list of functions; named according to the variables in the
\code{vars_sim}. Funtions are of the form \verb{function(df)} which will be used
to calculate model fit. If nor provided, uses mean absolute error (MAE).}

\item{weights}{a named vector; of weights for each variable in vars_sim. If not
provided, defaults to 1 for each variable.}

\item{ctrl}{list; from \code{\link{create_pest_control}}.}

\item{model_controls}{data.frame; model configuration, typically loaded via
\code{\link[AEME:get_model_controls]{get_model_controls()}}.}

\item{var_indices}{list; from \code{run_and_fit(return_indices = TRUE)}. Cached
so that every forward run skips re-deriving the netCDF date/depth
indices, exactly as \code{\link{calib_aeme}} does for its own workers.}

\item{include_wlev}{Logical. Include water level in the fit.}
}
\value{
Invisibly, the path of the generated script.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Writes the script PEST++ invokes for a single model evaluation, plus the
\code{.rds} payload it needs. The script itself is a three-line stub that calls
\code{\link{pest_forward_run}}; all of the logic lives in the package
where it can be tested and versioned, rather than in generated text.
}
\seealso{
\code{\link[=pest_forward_run]{pest_forward_run()}}
}
