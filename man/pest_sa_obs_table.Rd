% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_pst.R
\name{pest_sa_obs_table}
\alias{pest_sa_obs_table}
\title{Build the PEST observation table for a sensitivity analysis}
\usage{
pest_sa_obs_table(ctrl, weights)
}
\arguments{
\item{ctrl}{list; from \code{\link{create_sen_control}}.}

\item{weights}{named numeric; per-AEME-variable weights. Defaults to \code{1}
for every variable.}
}
\value{
A dataframe with columns \code{obsnme}, \code{obsval}, \code{weight}, \code{obgnme},
carrying a \code{map} attribute of \code{obsnme} / sub-region \code{name} / \code{var_aeme}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

One PEST "observation" per entry in \code{names(ctrl$vars_sim)} - i.e. per
sub-region that \code{\link{sa_aeme}} analyses separately (a
surface-temperature window, a bottom-temperature window, ...), not per
AEME variable. The forward run reports the corresponding \code{FUN_list}
value; \code{pestpp-sen} measures its sensitivity to the parameters. The
observed value is \code{0} and each observation is its own group, so the
Morris output is reported per sub-region.
}
\seealso{
\code{\link{pest_obs_table}}, \code{\link{create_sen_control}}
}
