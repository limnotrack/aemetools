% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calib_aeme_pest.R
\name{pest_posterior_runs}
\alias{pest_posterior_runs}
\title{Identify which logged runs form the solver's final (posterior) ensemble.}
\usage{
pest_posterior_runs(ctrl, param, res)
}
\arguments{
\item{ctrl}{list; from \code{\link{create_pest_control}}.}

\item{param}{dataframe; the parameters that were calibrated.}

\item{res}{dataframe; from \code{\link{read_pest_results}}, in the row
order it will be written to the database (row number becomes \code{run}).}
}
\value{
A dataframe of \code{run}, \code{realisation} and \code{iteration}, or a
zero-row dataframe when the solver wrote no ensembles.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The posterior parameter ensemble is the product of an iterative ensemble
smoother run - it is what gets pushed back through the model to give
predictive uncertainty - and it is \strong{not} recoverable by filtering the
results for the last generation.

The run log records every forward run, and each iteration contains the
lambda/scale-factor test runs on a subset of realisations as well as the
remainder run at the winning combination. Most of those candidates are
rejected. The accepted ensemble is specifically
\verb{<case>.<final>.par.csv}, a curated subset, so filtering by generation
returns a plausible-looking ensemble that is not the posterior.
}
