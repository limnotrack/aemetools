% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_functions.R
\name{fit_loss}
\alias{fit_loss}
\alias{nse_loss}
\alias{kge_loss}
\alias{kge_prime_loss}
\alias{log_kge_loss}
\title{Minimise-oriented (loss) variants of NSE / KGE fit functions}
\usage{
nse_loss(df)

kge_loss(df)

kge_prime_loss(df)

log_kge_loss(df)
}
\arguments{
\item{df}{dataframe; with columns \code{obs} (observed values) and \code{model}
(modelled values). Rows with \code{NA} in either column are dropped before
calculating.}
}
\value{
numeric; \verb{-1 *} the corresponding statistic (\code{-1} = perfect fit,
higher = worse fit).
}
\description{
\code{\link{nse}}, \code{\link{kge}}, \code{\link{kge_prime}} and
\code{\link{log_kge}} return their conventional statistic, where \code{1} is a
perfect fit and higher is better. \code{calib_aeme()} and \code{run_and_fit()}
instead \strong{minimise} the values returned by \code{FUN_list} entries, so these
\verb{_loss} companions return \verb{-1 *} the corresponding statistic (lower is
better, \code{-1} = perfect fit) and are what you pass in \code{FUN_list} for
calibration:
}
\details{
\preformatted{FUN_list <- list(HYD_temp = kge_loss, LKE_lvlwtr = rmse)}

\code{\link{mae}}, \code{\link{rmse}} and \code{\link{pbias}} are already
\code{0}-is-best, minimise-oriented, so they have no \verb{_loss} companion - use
them directly.
}
\examples{
df <- data.frame(obs = c(1, 2, 3, 4), model = c(1.1, 2.1, 2.9, 4.2))
nse_loss(df)
kge_loss(df)
kge_prime_loss(df)
log_kge_loss(df)

}
\seealso{
\code{\link{nse}}, \code{\link{kge}}, \code{\link{kge_prime}},
\code{\link{log_kge}} for the conventional (higher-is-better) statistics.
}
