% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pest_diagnostics.R
\name{pest_adjust_weights}
\alias{pest_adjust_weights}
\title{Rebalance observation weights from a completed run}
\usage{
pest_adjust_weights(obs_tbl, calib, mode = "discrepancy", iteration = NULL)
}
\arguments{
\item{obs_tbl}{dataframe; from \code{\link{pest_obs_table}}, carrying its
\code{map} attribute. Its \code{weight} column is what gets rescaled.}

\item{calib}{The completed run - as accepted by \code{\link{read_pest_phi}}.}

\item{mode}{Character. Only \code{"discrepancy"} is implemented.}

\item{iteration}{Integer or \code{NULL}. Which iteration's simulated ensemble
to measure the misfit at. \code{NULL} (default) uses the last (posterior).}
}
\value{
\code{obs_tbl} with a rescaled \code{weight} column and its \code{map}
attribute preserved, ready to pass to a second \code{\link{calib_aeme}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

After a first \code{pestpp-ies} run, rescale each observation group's weights
so that its contribution to phi is on the order of the number of
(non-zero-weight) observations in the group - the "discrepancy" rule from
\code{pyemu.Pst.adjust_weights_discrepancy()}. A group the model fits far
better than its weights imply gets down-weighted, and vice versa, so a
second run balances effort across variables instead of chasing whichever
group happened to start with the largest misfit.
}
\seealso{
\code{\link[=pest_obs_table]{pest_obs_table()}}, \code{\link[=read_pest_phi_group]{read_pest_phi_group()}}
}
