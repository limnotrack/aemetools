% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pest_prior.R
\name{pest_prior_cov}
\alias{pest_prior_cov}
\title{Build a prior parameter covariance matrix}
\usage{
pest_prior_cov(param, sigma_range = 4, file = NULL)
}
\arguments{
\item{param}{dataframe; as passed to \code{\link{calib_aeme}} - see
\code{\link{pest_param_table}} for the required columns.}

\item{sigma_range}{Numeric. Number of standard deviations spanned by the
\verb{[min, max]} range. Default \code{4}, the \code{pyemu} default (so \code{sd} is a
quarter of the range, or of the log10 range for a log parameter).}

\item{file}{Character or \code{NULL}. When given, the matrix is also written to
this path in PEST ASCII matrix format.}
}
\value{
Invisibly, a square numeric matrix with \code{parnme} row/column names
(\code{p001}, \code{p002}, ...), carrying the same \code{map} attribute as
\code{\link{pest_param_table}} so the synthetic names can be related back
to the aemetools parameters.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Turns the calibration bounds into a diagonal prior parameter covariance,
the way \code{pyemu.Cov.from_parameter_data()} does: each parameter's standard
deviation is a fixed fraction of its feasible range, so that the range
spans \code{sigma_range} standard deviations. Log-transformed parameters are
treated in log10 space, matching how \code{\link{pest_param_table}} hands
them to PEST++.

The result is written in the PEST ASCII matrix format and can be passed to
\code{pestpp-ies} as \code{++parcov(...)} (via \code{prior_cov} in
\code{\link{create_pest_control}}) so the prior ensemble is drawn from a
Gaussian rather than from independent uniforms on the bounds.
}
\examples{
param <- data.frame(model = "glm_aed", file = "glm3.nml",
                    group = "light", name = c("Kw", "ce"),
                    index = c(1, NA), value = c(0.5, 0.0013),
                    min = c(0.1, 0.0005), max = c(1.5, 0.005),
                    log = c(TRUE, FALSE))
pest_prior_cov(param)
}
\seealso{
\code{\link[=pest_prior_ensemble]{pest_prior_ensemble()}}, \code{\link[=create_pest_control]{create_pest_control()}}
}
