% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_hydro_model.R
\name{make_hydro_model}
\alias{make_hydro_model}
\title{Create a spatial HydroModel object}
\usage{
make_hydro_model(
  id,
  reaches,
  lake,
  catchments,
  dem = NULL,
  land_cover = NULL,
  soil = NULL,
  outlet = NULL,
  plot = FALSE
)
}
\arguments{
\item{id}{numeric; Reach ID (\code{nzsegment}).}

\item{reaches}{sf; object with reaches as linestrings.}

\item{lake}{sf; polygon of the target lake shore.}

\item{catchments}{sf; polygon of catchments (including sub-catchments).}

\item{dem}{ANY; optional DEM raster (e.g. a \code{terra::SpatRaster}).
Defaults to \code{NULL}.}

\item{land_cover}{ANY; optional land cover data. Defaults to \code{NULL}.}

\item{soil}{ANY; optional soil data. Defaults to \code{NULL}.}

\item{outlet}{ANY; optional outlet point(s) as an \code{sf} object.
Defaults to \code{NULL}.}

\item{plot}{logical; plot the assembled reaches, lake and catchment?
Defaults to \code{FALSE}.}
}
\value{
A \code{\linkS4class{HydroModel}} object with spatial slots populated and
\code{catchment_area} computed.  The airGR input slots (\code{inputs_model},
\code{data}, \code{start}, \code{fun_mod}, \code{fun_mod_name}) are \code{NULL} / \code{NA} until
\code{\link{make_GR_inputs}()} is called.
}
\description{
Assembles the upstream river-channel network and catchment polygon for a
given reach ID, computes the total catchment area (excluding the lake
polygon), and returns a \code{\linkS4class{HydroModel}} object whose slots are focused on
spatial data.  The computed airGR model inputs are added in a subsequent
call to \code{\link{make_GR_inputs}()}.
}
\examples{
\dontrun{
data_dir   <- system.file("extdata/hydro/", package = "aemetools")
lake       <- readRDS(file.path(data_dir, "lake.rds"))
reaches    <- readRDS(file.path(data_dir, "reaches.rds"))
catchments <- readRDS(file.path(data_dir, "catchments.rds"))

hm <- make_hydro_model(id = 4087861, reaches = reaches, lake = lake,
                       catchments = catchments, plot = TRUE)
hm
}
}
