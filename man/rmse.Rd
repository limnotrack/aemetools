% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_functions.R
\name{rmse}
\alias{rmse}
\title{Root Mean Square Error (RMSE) fit function}
\usage{
rmse(df)
}
\arguments{
\item{df}{dataframe; with columns \code{obs} (observed values) and \code{model}
(modelled values). Rows with \code{NA} in either column are dropped before
calculating.}
}
\value{
numeric; root mean square error, in the same units as
\code{obs}/\code{model}.
}
\description{
Calculates the root mean square error between observed and modelled
values. Already \code{0} (perfect fit) at its best and increasing as fit
worsens, so - unlike \code{\link{nse}}/\code{\link{kge}} - it is
returned as-is, with no negation needed to match \code{FUN_list}'s
minimise-the-returned-value convention.
}
\details{
Like \code{\link{mae}}, RMSE stays in the variable's native units and so
is not directly comparable across variables with different units or
magnitudes when combined in a multi-variable \code{FUN_list} - see
\code{\link{mae}} for details. RMSE additionally squares errors before
averaging, so - like NSE - it weights large deviations more heavily than
MAE does.
}
\examples{
df <- data.frame(obs = c(1, 2, 3, 4), model = c(1.1, 2.1, 2.9, 4.2))
rmse(df)

}
