% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pest.R
\name{plot_pest_phi_group}
\alias{plot_pest_phi_group}
\title{Plot the per-group objective-function trajectory}
\usage{
plot_pest_phi_group(ctrl, log_y = TRUE)
}
\arguments{
\item{ctrl}{The PEST++ run to read. Usually the object returned by
\code{\link{read_calib}}, which carries the resolved run directory
in its metadata. A directory path also works, as does a
\code{\link{create_pest_control}} object when \code{pest_dir} was given
as an absolute path - a relative \code{pest_dir} is resolved against the
lake directory when the run starts, so the control on its own does
not know where the files ended up.}

\item{log_y}{Logical. Log-scale the phi axis? Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

One line per observation group (i.e. per \code{vars_sim} variable) of its phi
contribution against iteration. Divergent lines - one variable falling,
another flat or rising - say the run is trading fit in one variable for
another, which the total phi in \code{\link{plot_pest_phi}} cannot show.
}
\seealso{
\code{\link[=read_pest_phi_group]{read_pest_phi_group()}}, \code{\link[=plot_pest_phi]{plot_pest_phi()}}
}
